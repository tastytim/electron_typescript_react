import { styled } from "@mui/system";
import { NavLink } from "react-router-dom";


export const NavLinStyled = styled(NavLink)({
    fontSize:'16px',
    textDecoration:'none',
    color:'white',
    fontWeight:'extrabold',
    textTransform:'uppercase'
  })